.DS_Store
*.swp
.idea/
.vscode/

# Design bundles / Finder artifacts
Design/*.zip
.thumbnail

# Research inputs — local only, never published: the competitor crawl (the
# committed catalog seed is backend/repairs/seed/catalog.json) and the hero-film
# keyframes and clips (the site ships the frames built from them in
# frontend/public/applelab/film/)
research/
